[pytest]
asyncio_mode = auto
addopts = -q -m "not integration and not e2e" -k "not threads_extractor and not threads_url and not twitter_extractor"
markers =
    integration: marks tests as integration (skipped by default)
    e2e: marks tests as end-to-end (skipped by default)
